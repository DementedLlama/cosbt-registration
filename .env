# Loaded by Prisma CLI (via prisma.config.ts) and Next.js.
# .env.local overrides these values for Next.js at runtime.
# Commit this file — keep secrets in .env.local (gitignored).

DATABASE_URL="postgresql://cosbt:cosbt@localhost:5432/cosbt_camp"
